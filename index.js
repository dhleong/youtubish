const { CredentialsBuilder } = require('./lib/creds');
const { WatchHistory } = require('./lib/history');
const { YoutubePlaylist } = require('./lib/playlist');

module.exports = {
    CredentialsBuilder,
    WatchHistory,
    YoutubePlaylist,
};
